### End to End Chatbot AI

# Agentic‑ChatBot

> A simple AI chatbot built with Python and Streamlit. Provides an interface to chat using an API key, without storing any conversation history.

## 🧠 What is Agentic‑ChatBot?

Agentic‑ChatBot is a **basic, stateless chatbot** application.  
It allows users to chat with an AI model via an API key using a Streamlit web interface.  
**Important:** It does not remember previous messages — each input is processed independently.

---

## ⚙️ Tech Stack & Dependencies

- **Python** (3.8+)  
- **Streamlit** — for the web UI  
- API access (via your preferred AI model or service)  
- Other Python packages (as required; install via `requirements.txt`)
